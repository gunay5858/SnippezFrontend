import { BasicListResponse } from './basic-list-response';

describe('BasicListResponse', () => {
  it('should create an instance', () => {
    expect(new BasicListResponse()).toBeTruthy();
  });
});
