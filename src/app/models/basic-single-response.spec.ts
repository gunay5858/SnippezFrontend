import { BasicSingleResponse } from './basic-single-response';

describe('BasicSingleResponse', () => {
  it('should create an instance', () => {
    expect(new BasicSingleResponse()).toBeTruthy();
  });
});
